import { teamsActions } from './Teams';
import { timerActions } from './Timer';
import { wordsActions } from './Words';

const actions = {
  teams: teamsActions,
  timer: timerActions,
  words: wordsActions,
};

export default actions;
